// ============================================================
// LavOS 2026 — vscode-extension/extension.js
// VS Code extension: sends the current file + error/selection
// to Vision, shows the AI suggestion inline.
// Extension talks to the same local Vision (no cloud needed).
// Todo(phase 11): command "Ask Vision", read active editor text,
// call brain over localhost, render answer in a webview panel.
// ============================================================