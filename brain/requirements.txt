# ============================================================
# LavOS 2026 — brain Python dependencies
# Keep versions loose so we can install anywhere fast.
# Todo(phase 0/1): fill in the real list once tools are chosen:
# ollama, openai (for NIM/Gemini OpenAI-compat), whisper,
# porcupine (wake word), pyttsx3, requests, pyscreenshot/ocr,
# python-dotenv. Install with: pip install -r requirements.txt
# ============================================================