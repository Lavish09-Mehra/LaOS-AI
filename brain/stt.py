# ============================================================
# LavOS 2026 — brain/stt.py  (hearing)
# Speech-to-text: converts your voice into text for Vision.
# Offline Whisper (small) so it works with no internet.
# Todo(phase 8): mic capture -> Whisper transcribe -> return text;
# handle quiet/garbled audio gracefully; hotkey push-to-talk path.
# ============================================================